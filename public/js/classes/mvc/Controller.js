/**
 * The controller of the game, it need to know the view and the model
 * @param view {View}
 * @param model {Model}
 */
 
function Controller(view, model){
    var _view = view;
    var _model = model;
}